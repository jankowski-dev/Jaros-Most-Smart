# Инструкция по тестированию Yandex SpeechKit на Railway

## Проблема, которую вы обнаружили

Вы тестируете приложение **локально** (file:// URL), а не на **Railway**. Вот что происходит:

1. **URL теста**: `file:///C:/Users/user/Desktop/YaApp_2/speech-test.html`
   - Это локальный файл, а не развернутое приложение
   - Локальные файлы не могут делать запросы к удаленным серверам из-за CORS

2. **Конфигурация по умолчанию**: `engine: 'browser'`
   - В `config.js` установлено значение по умолчанию "browser"
   - Переменные среды Railway не загружаются локально

3. **Ошибка "Failed to fetch"**:
   - Локальный файл пытается подключиться к `https://jms.up.railway.app`
   - Браузер блокирует эти запросы из-за политики безопасности

## Как правильно тестировать

### Шаг 1: Разверните приложение на Railway

1. Убедитесь, что все файлы загружены в ваш Railway проект:
   - `railway.json`
   - `speech-proxy.js` 
   - `package.json`
   - Все остальные файлы проекта

2. Добавьте переменные среды в Railway:
   - `SPEECH_ENGINE` = `yandex`
   - `YANDEX_SPEECH_API_KEY` = ваш API ключ Yandex
   - `YANDEX_SPEECH_FOLDER_ID` = ваш Folder ID
   - `YANDEX_PROXY_URL` = `https://ваш-домен.railway.app/api/tts`

3. Перезапустите деплой

### Шаг 2: Откройте тестовую страницу на Railway

**НЕ открывайте локальный файл!** Вместо этого откройте:

```
https://ваш-домен.railway.app/speech-test.html
```

### Шаг 3: Проверьте конфигурацию

На тестовой странице нажмите "Проверить конфигурацию". Должно отобразиться:
- ✅ Конфигурация загружена
- Движок: **yandex** (не browser!)
- Прокси URL: установлен

### Шаг 4: Проверьте сервер

Нажмите "Проверить сервер". Должно отобразиться:
- ✅ Сервер работает: ok (speech-proxy)

### Шаг 5: Тестируйте синтез

1. Выберите "Yandex SpeechKit" в выпадающем списке
2. Нажмите "Тестировать синтез"
3. Проверьте консоль браузера (F12 → Console)

### Шаг 6: Проверьте прокси

Нажмите "Тест прокси". Должно отобразиться:
- ✅ Прокси работает: OK

## Что должно произойти при правильной настройке

### В консоли браузера вы увидите:
```
[SpeechService 2026-02-18T08:00:00.000Z] ℹ️ Запуск синтеза речи: "Привет! Это тестовая фраза..."
[SpeechService 2026-02-18T08:00:00.100Z] 🔍 Начинаем Yandex SpeechKit синтез
[SpeechService 2026-02-18T08:00:00.200Z] 🔍 Отправляем запрос к прокси
[SpeechService 2026-02-18T08:00:00.500Z] 🔍 Получен ответ от прокси
[SpeechService 2026-02-18T08:00:00.600Z] ✅ Аудио получено успешно
[SpeechService 2026-02-18T08:00:00.700Z] ✅ Синтез речи выполнен успешно
```

### На тестовой странице:
- Статус: "✅ Синтез выполнен успешно"
- В журнале событий: успешные сообщения
- Вы услышите речь через Yandex SpeechKit (не браузерный синтез!)

## Быстрая проверка конфигурации

Чтобы быстро проверить, какая конфигурация используется, откройте консоль браузера на Railway и выполните:

```javascript
console.log('Текущая конфигурация:', window.appConfig.speech);
console.log('Движок:', window.appConfig.speech.engine);
```

Если движок показывает "browser", значит:
1. Переменная среды `SPEECH_ENGINE` не установлена в Railway
2. Или значение установлено как "browser"

## Устранение неполадок

### Проблема: Движок все равно показывает "browser"
**Решение**: Проверьте переменные среды в Railway:
1. Откройте настройки проекта в Railway
2. Перейдите в Variables
3. Убедитесь, что `SPEECH_ENGINE` = `yandex`
4. Перезапустите деплой

### Проблема: Ошибка прокси-сервера
**Решение**: Проверьте логи Railway:
1. В Railway откройте Logs
2. Ищите ошибки в speech-proxy.js
3. Убедитесь, что API ключ Yandex действителен

### Проблема: Нет звука, но ошибок нет
**Решение**: Проверьте аудиовыход:
1. Убедитесь, что звук включен в системе
2. Проверьте, что браузер имеет разрешение на воспроизведение звука
3. Попробуйте другой браузер

## Важные моменты

1. **Локальное тестирование Yandex SpeechKit невозможно** из-за CORS
2. **Все тесты должны выполняться на развернутом приложении** на Railway
3. **Конфигурация загружается из переменных среды Railway**, а не из локального файла
4. **По умолчанию используется браузерный синтез**, пока вы не установите `SPEECH_ENGINE=yandex`

## Следующие действия

1. Разверните обновленный проект на Railway
2. Установите правильные переменные среды
3. Откройте `https://ваш-домен.railway.app/speech-test.html`
4. Выполните все проверки
5. Пришлите мне результаты из консоли браузера

Если после этих шагов проблема сохранится, пришлите:
1. Скриншот консоли браузера
2. Логи из Railway
3. Текущую конфигурацию (из console.log)