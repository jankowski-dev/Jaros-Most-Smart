# Проверка работы основного приложения

## Цель проверки
Убедиться, что основное приложение "Ярик.Уроки" использует Yandex SpeechKit вместо браузерного синтеза.

## Шаги проверки

### 1. Откройте основное приложение
```
https://ваш-домен.railway.app/
```

### 2. Проверьте конфигурацию в консоли браузера
Откройте DevTools (F12) → вкладка Console, выполните:

```javascript
// Проверка 1: Какая конфигурация загружена
console.log('=== ПРОВЕРКА КОНФИГУРАЦИИ ===');
console.log('Движок синтеза:', window.appConfig?.speech?.engine);
console.log('Включен:', window.appConfig?.speech?.common?.enabled);
console.log('Прокси URL:', window.appConfig?.speech?.yandex?.proxyUrl);

// Проверка 2: Инициализирован ли SpeechService
console.log('=== ПРОВЕРКА SPEECH SERVICE ===');
console.log('SpeechService доступен:', typeof SpeechService !== 'undefined');
console.log('speechService инициализирован:', !!window.speechService);

if (window.speechService) {
    console.log('Текущий движок speechService:', window.speechService.config.engine);
    console.log('Режим отладки:', window.speechService.debug);
}
```

### 3. Проверьте работу озвучки в приложении

#### Способ A: Через интерфейс приложения
1. Запустите любой урок (математика, чтение и т.д.)
2. Нажмите на кнопку озвучки (иконка динамика)
3. Прислушайтесь к голосу:
   - **Браузерный синтез**: обычно роботизированный, стандартный голос ОС
   - **Yandex SpeechKit**: более естественный голос "Алена" с эмоциями

#### Способ B: Прямой тест в консоли
```javascript
// Тест 1: Проверка доступности speechService
if (window.speechService) {
    console.log('Запускаем тестовый синтез через speechService...');
    window.speechService.speak('Привет, это тест Yandex SpeechKit');
}

// Тест 2: Проверка через глобальную функцию speakText (если есть)
if (typeof speakText === 'function') {
    console.log('Запускаем тест через speakText...');
    speakText('Тестовая фраза для проверки');
}
```

### 4. Проверьте логи в консоли
Во время работы озвучки следите за консолью. Должны появиться логи:

**Если работает Yandex SpeechKit:**
```
[SpeechService 2026-02-18T08:00:00.000Z] ℹ️ Запуск синтеза речи: "Привет, это тест..."
[SpeechService 2026-02-18T08:00:00.100Z] 🔍 Начинаем Yandex SpeechKit синтез
[SpeechService 2026-02-18T08:00:00.200Z] 🔍 Отправляем запрос к прокси
[SpeechService 2026-02-18T08:00:00.500Z] 🔍 Получен ответ от прокси
[SpeechService 2026-02-18T08:00:00.600Z] ✅ Аудио получено успешно
```

**Если работает браузерный синтез:**
```
[SpeechService 2026-02-18T08:00:00.000Z] ℹ️ Запуск синтеза речи: "Привет, это тест..."
[SpeechService 2026-02-18T08:00:00.100Z] 🔍 Используем браузерный синтез
```

### 5. Проверьте сетевые запросы
В DevTools откройте вкладку Network:
1. Очистите логи (кнопка 🗑️)
2. Запустите озвучку в приложении
3. Ищите запросы к `/api/tts`

**Должен быть POST запрос:**
- URL: `https://ваш-домен.railway.app/api/tts`
- Method: POST
- Status: 200 OK
- Type: audio/ogg или audio/mpeg

## Критерии успеха

### ✅ Приложение работает с Yandex SpeechKit если:
1. Конфигурация показывает `engine: 'yandex'`
2. В консоли есть логи о Yandex синтезе
3. В Network есть POST запросы к `/api/tts`
4. Голос звучит естественно (Yandex "Алена")

### ❌ Приложение использует браузерный синтез если:
1. Конфигурация показывает `engine: 'browser'`
2. В консоли нет запросов к прокси
3. Голос звучит как стандартный синтез ОС

## Что делать если приложение все еще использует браузерный синтез

### Проблема 1: Конфигурация показывает `engine: 'browser'`
**Решение:** Проверьте переменные среды в Railway:
1. `SPEECH_ENGINE` должен быть `yandex`
2. Перезапустите деплой
3. Очистите кэш браузера (Ctrl+F5)

### Проблема 2: SpeechService не инициализирован
**Решение:** Проверьте загрузку скриптов:
1. Убедитесь, что `config.js` загружается перед `speech-service.js`
2. Убедитесь, что `speech-service.js` загружается перед `app.js`
3. Проверьте консоль на ошибки загрузки скриптов

### Проблема 3: Нет запросов к `/api/tts`
**Решение:** Проверьте инициализацию:
```javascript
// В консоли проверьте
console.log('appConfig:', window.appConfig);
console.log('speechService:', window.speechService);

// Если speechService не создан, создайте его вручную
if (window.appConfig && !window.speechService) {
    window.speechService = new SpeechService(window.appConfig.speech);
    console.log('SpeechService создан вручную');
}
```

## Быстрый тест

Выполните этот код в консоли для быстрой проверки:

```javascript
// Быстрая проверка всей цепочки
function quickTest() {
    console.log('=== БЫСТРЫЙ ТЕСТ YANDEX SPEECHKIT ===');
    
    // 1. Проверка конфигурации
    if (!window.appConfig) {
        console.error('❌ appConfig не загружен');
        return;
    }
    
    const engine = window.appConfig.speech?.engine;
    console.log(`1. Движок: ${engine} ${engine === 'yandex' ? '✅' : '❌'}`);
    
    // 2. Проверка SpeechService
    if (!window.speechService && typeof SpeechService !== 'undefined') {
        window.speechService = new SpeechService(window.appConfig.speech);
        console.log('2. SpeechService создан ✅');
    } else if (window.speechService) {
        console.log('2. SpeechService уже инициализирован ✅');
    } else {
        console.error('❌ SpeechService не доступен');
        return;
    }
    
    // 3. Тестовый синтез
    console.log('3. Запускаем тестовый синтез...');
    window.speechService.speak('Тест Yandex SpeechKit').then(() => {
        console.log('✅ Синтез выполнен успешно!');
    }).catch(error => {
        console.error('❌ Ошибка синтеза:', error.message);
    });
}

// Запустите тест
quickTest();
```

## Итоговая проверка

После всех проверок ответьте на вопросы:

1. **Конфигурация показывает `engine: 'yandex'`?** Да/Нет
2. **В консоли есть логи о Yandex синтезе?** Да/Нет  
3. **В Network есть запросы к `/api/tts`?** Да/Нет
4. **Голос звучит через Yandex (не браузерный)?** Да/Нет

Если на все вопросы **Да** - интеграция Yandex SpeechKit успешно завершена! 🎉

Если есть проблемы - пришлите:
1. Скриншот консоли браузера
2. Результаты быстрого теста
3. Логи из вкладки Network